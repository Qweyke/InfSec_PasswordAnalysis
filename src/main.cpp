#include <string>
#include <Windows.h>
#include <iostream>

uint32_t checkPswdStrength(std::string pswd)
{
	return 0;
}

uint32_t countHackTime(uint32_t speed, uint32_t tries, uint32_t lockTime)
{
	return 0;
}

int main()
{
	UINT ecoding = GetConsoleOutputCP();
	std::cout << ecoding << "\n";
	char inp;
	std::cin >> inp;
	return 0;
}